# Knowledge Base

This directory holds markdown documents used by `md_lookup`.

## Usage
- Add domain documents (e.g., `architecture.md`, `api.md`).
- Use clear headers: `# Title`, `## Section`.
- The lookup tool scores sections by keyword frequency.

## Example
### Sample Section
This sample demonstrates how the lookup collects sections.
